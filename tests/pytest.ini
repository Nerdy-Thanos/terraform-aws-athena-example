[pytest]
markers =
    integration: real-AWS integration tests
    security: asserts the access path is locked down (IAM least-privilege, private networking)
    functional: invokes the deployed Lambda and asserts real query behaviour
