apiVersion: v1
kind: ServiceAccount
metadata:
  name: athena-query-runner
  namespace: default   # must match k8s_namespace in terraform.tfvars
  annotations:
    # IRSA binding. Set to the Terraform output: `terraform output eks_query_role_arn`
    eks.amazonaws.com/role-arn: <EKS_QUERY_ROLE_ARN>
